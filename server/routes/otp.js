// OTP functionality removed as requested.
const express = require('express');
const router = express.Router();

// Placeholder route
router.get('/', (req, res) => {
  res.json({ message: 'OTP routes removed.' });
});

module.exports = router;
